import styled from 'styled-components';

const WrapperSinglePage = styled.div`
    // display: grid;
    // grid-template-columns: 1fr 1fr;
`
const ThumbSwiper = styled.div`
    width: 50%;
    height: 200px;
`
const ThumbImg = styled.div`
    width: 100%;
    height: 200px;
`
const StyledImg = styled.img`
    display: block;
    width: 100%;
    height: 100%;
    object-fit: cover;
`

const HouseDetails = styled.div`

`
export {WrapperSinglePage, ThumbSwiper, ThumbImg, StyledImg, HouseDetails}